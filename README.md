# 🗺️ 역사 건축 지도 (Historical Architecture Map)

지리 정보 시스템(GIS)을 활용하여 내 주변의 문화유산을 탐색하고, 나만의 답사 코스를 생성하는 교육용 웹 애플리케이션이다.

## 🚀 시작하기 (필수)

이 프로젝트는 **Kakao Maps API**를 사용합니다. 지도를 화면에 표시하려면 반드시 API 키가 필요합니다.

1. **Kakao Developers 가입 및 키 발급**
   - [Kakao Developers](https://developers.kakao.com/) 접속
   - '내 애플리케이션' -> '애플리케이션 추가'
   - '플랫폼' 설정에서 사이트 도메인 등록 (로컬 테스트 시 `http://127.0.0.1:5500` 등 등록)
   - **JavaScript 키** 복사

2. **키 입력**
   - `index.html` 파일을 엽니다.
   - 아래 코드를 찾아 `YOUR_APP_KEY` 부분을 발급받은 키로 변경합니다.
   ```html
   <script type="text/javascript" src="//dapi.kakao.com/v2/maps/sdk.js?appkey=YOUR_APP_KEY"></script>
   ```

3. **실행**
   - `index.html` 파일을 브라우저에서 실행합니다. (Live Server 권장)

## ✨ 주요 기능

1. **지역별 문화유산 탐색**
   - 경주, 서울, 부여 등 주요 역사 도시를 선택하여 지도 이동 기능을 제공합니다.

2. **시대별 타임라인 필터**
   - 하단 슬라이더를 조절하여 삼국시대, 통일신라, 조선시대 등 특정 시대의 건축물만 필터링합니다.

3. **맞춤형 답사 코스 추천**
   - "답사 코스 추천받기" 버튼을 클릭하면, 현재 선택된 지역의 문화유산들을 연결하여 최적의 동선을 제안합니다.
   - **알고리즘**: 유산 간 거리 20km 이내, 최대 3곳 방문 조건을 만족하는 경로를 생성합니다.

## 📂 파일 구조

- `index.html`: 웹사이트 구조 및 화면 레이아웃
- `style.css`: 한국적 미를 살린 모던한 디자인 스타일 시트
- `js/data.js`: 문화유산 위치 및 정보 데이터 (JSON 형식)
- `js/app.js`: 지도 제어, 필터링 로직, 코스 추천 알고리즘 구현
